// Bundled logo URL — works both online (CDN) and offline (local install).
import missyLogoUrl from "@/assets/missy-logo.png";

export const logoUrl = missyLogoUrl;
export default { url: missyLogoUrl };
